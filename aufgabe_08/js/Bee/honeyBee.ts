namespace a08_Canvas {

    export class HoneyBee extends Bee {
        constructor(_x: number, _y: number, _size: number, _color: string) {
            super(_x, _y, _size, _color);
        }
        
        move(): void {
         // zu den Blumen und zurück fiegen.   
        }
    }

}